build:
  hello-world:
    image: okteto.dev/java-gradle-hello-world:1.0.0
    context: .
deploy:
  - kubectl apply -f k8s.yml
dev:
  hello-world:
    command: java -jar *.jar
    sync:
      - .:/usr/src/app
    forward:
      - 8080:8080
